{
    "name": "cs4920-project",
    "version": "0.1.0",
    "repository": "https://github.com/mezzode/cs4920-project.git",
    "license": "AGPL-3.0-or-later",
    "scripts": {
        "build": "yarn clean && yarn --cwd web build && yarn --cwd server build",
        "start:prod": "yarn build && node server/build/index.js",
        "start": "docker-compose up",
        "clean": "rimraf web/build && rimraf server/build"
    },
    "devDependencies": {
        "rimraf": "^2.6.2"
    }
}
