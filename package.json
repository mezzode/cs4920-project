{
  "name": "cs4920-project",
  "version": "0.1.0",
  "repository": "https://github.com/mezzode/cs4920-project.git",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean:build && cd web && yarn build && cd ../server && yarn build && cd ..",
    "start:prod": "yarn clean:db && docker-compose up",
    "clean:build": "rimraf web/build && rimraf server/build",
    "clean:db": "rimraf database/db && mkdirp database/db"
  },
  "devDependencies": {
    "mkdirp": "^0.5.1",
    "rimraf": "^2.6.2"
  }
}
